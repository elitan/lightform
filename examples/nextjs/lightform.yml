name: lightform-example-nextjs

ssh:
  username: lightform

apps:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    server: 157.180.25.101
    environment:
      plain:
        - NODE_ENV=production
    proxy:
      #hosts:
      #  - nextjs.example.mylightform.cloud
      app_port: 3000
