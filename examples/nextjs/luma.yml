name: luma-example-nextjs

ssh:
  username: luma

apps:
  web:
    image: elitan/luma-example-nextjs
    build:
      context: .
      dockerfile: Dockerfile
      platform: linux/amd64
    servers:
      - 157.180.25.101
    environment:
      plain:
        - NODE_ENV=production
    proxy:
      hosts:
        - nextjs.example.myluma.cloud
      app_port: 3000
