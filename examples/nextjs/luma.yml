name: luma-example-nextjs

ssh:
  username: luma

apps:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    server: 157.180.25.101
    environment:
      plain:
        - NODE_ENV=production
    proxy:
      hosts:
        - nextjs.example.myluma.cloud
      app_port: 3000
